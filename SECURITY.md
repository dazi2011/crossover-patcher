# Security policy

Please report security issues through a private GitHub security advisory. Do not
include game logs, account identifiers, device identifiers, cookies, tokens, or
absolute home-directory paths in a public issue.

The patcher refuses unknown CrossOver builds and never modifies game or ACE
binaries. A failed signature, version, hash, transaction, or rollback check is a
hard failure and must not be bypassed.
